server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;
  # registry JSON is fetched by `shadcn add` from other repos
  location /r/ { add_header Access-Control-Allow-Origin *; try_files $uri =404; }
  location / { try_files $uri /index.html; }
}
